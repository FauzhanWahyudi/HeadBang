<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cart</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

</head>
<body class="text-center">
    <header>
        <%- include('nav') %>
    </header>
    <h1 class="text-center">Shoping Cart</h1>
    <br>
    <br>
    <h3>On Cart Product List :</h3>
    <% totalPrice = 0 %>
    <% if (user.Carts[0].Products && user.Carts[0].Products.length) { %>
        <div class="container">
            <table class="table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Price</th>
                    </tr>
                </thead>
                <tbody>
                    <% user.Carts[0].Products.forEach(el => { %>
                        <tr>
                            <td><%= el.name %></td>
                            <td><%= toIDR(el.price) %></td>
                        </tr>
                        <% totalPrice += el.price %>
                    <% }) %>
                </tbody>
            </table>
                 <h3 class="h3 touppercase text-center">Your Total Bill: <%= toIDR(totalPrice) %></h3>
        </div>
        <% } else { %>
            <h2 class="h2 touppercase text-center">You have no products in cart</h2>
        <% } %>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>